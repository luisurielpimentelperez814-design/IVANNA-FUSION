# DESHABILITADO — este workflow usaba src/ (directorio huérfano eliminado).
# La compilación del módulo Magisk ahora la hace el job build-magisk
# dentro de .github/workflows/ivanna-ci.yml, que recoge libomega_effect.so
# del job build-apk vía artifacts.
name: Build IVANNA FUSION Magisk Module (DEPRECATED)
on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'Este workflow está deshabilitado. Usa ivanna-ci.yml'
        required: true
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow deshabilitado. Ver ivanna-ci.yml"

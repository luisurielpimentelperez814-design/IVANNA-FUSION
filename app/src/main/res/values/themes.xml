<resources>
    <!--
        Tema base usado solo en AndroidManifest mientras carga la actividad.
        El UI real se dibuja con Compose Material3 (MaterialTheme) en MainActivity,
        por eso este tema parte de un estilo nativo de Android (sin dependencias extra).
    -->
    <style name="Theme.IVANNAFusion" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
    </style>
</resources>
